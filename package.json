{
  "type": "module",
  "scripts": {
    "build": "plain-blog",
    "build:ci": "CI=true plain-blog",
    "watch": "node --watch ./node_modules/.bin/plain-blog",
    "serve": "serve dist"
  },
  "dependencies": {
    "plain-blog": ">=0.6.1",
    "sanitize.css": "^13.0.0",
    "serve": "^14.2.4"
  }
}
